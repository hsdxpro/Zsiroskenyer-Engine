struct VS_OUT {

  float4 position : POSITION;

  float4 color    : COLOR;

};

VS_OUT VS_MAIN(float2 position : POSITION)
{

  VS_OUT OUT;

  OUT.position = float4(position, 0, 1);

  OUT.color    = float4(0, 1, 0, 1);  // RGBA green

  return OUT;

}